# Example environment variables
VITE_API_BASE_URL=https://api.example.com
PUBLIC_URL=/
# Add any other environment variables here used by the app
# Example env vars for Cometapi proxy
# DO NOT COMMIT REAL KEYS. Set this in your deployment environment or local .env (not checked into git).
COMETAPI_KEY=your_cometapi_key_here
# The external Cometapi endpoint your proxy will call (set on the server)
COMETAPI_ENDPOINT=https://api.cometapi.example/v1/chat
# Optional system prompt your proxy will prepend to conversations (server-side only)
COMET_SYSTEM_PROMPT=You are a supportive assistant for introverted users. Be empathetic, concise, and avoid clinical diagnosis. Provide tips to recharge and gentle suggestions.
# Client should point to the deployed proxy URL, e.g. https://your-site.vercel.app/api/comet
VITE_CHAT_PROXY_URL=/api/comet
